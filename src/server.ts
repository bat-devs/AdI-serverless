import app from "./app";

const port = process.env.PORT;

app.listen(port, () => {
    console.log(`Server's running on http://localhost:${port}`);
    console.log(`Server's running on http://localhost:${port}`);
    console.log(`Server's running on http://localhost:${port}`);
    console.log(`Server's running on http://localhost:${port}`);
});
